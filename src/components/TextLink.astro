---
export interface Props {
    href: string;
    className?: string;
    ariaLabel?: string;
    title?: string;
    disabled?: boolean;
    color?: string;
    underline?: string;
}

const {
    href,
    className,
    ariaLabel,
    title,
    disabled = false,
    underline = "false",
    color = "text-ctp-sapphire",
} = Astro.props;

const underlineClasses =
    underline == "true" ? "underline underline-offset-[5px]" : "";
---

<a
    href={disabled ? "#" : href}
    tabindex={disabled ? "-1" : "0"}
    class={`group inline-block link link-underline text-mono ${className} ${color} ${underlineClasses}`}
    aria-label={ariaLabel}
    title={title}
    aria-disabled={disabled}
>
    <slot />
</a>

<style>
    @reference "@styles/base.css";
    .link {
        @apply font-semibold italic
        transition-colors duration-100 ease-out hover:text-dark-sapphire hover:no-underline;
        /* underline underline-offset-[5px]  */
    }

    .link-underline {
        @apply bg-gradient-to-r from-ctp-sapphire to-dark-sapphire bg-[length:0%_3px] bg-left-bottom bg-no-repeat transition-all duration-75 ease-out hover:bg-[length:100%_2px];
    }
</style>
