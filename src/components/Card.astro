---
import { stripInlineMarkdown } from "@utils/inlineMarkdown";

export interface Props {
    title: string;
    date: Date;
    url: string;
}

const dateOptions: Intl.DateTimeFormatOptions = {
    year: "numeric",
    month: "long",
    day: "numeric",
};

const { title, date, url } = Astro.props;
const plainTitle = stripInlineMarkdown(title);
---

<a href={url}>
    <div class="card-container">
        <div class="post-title flex justify-start">
            <code>&nbsp;</code><div>{plainTitle}</div>
        </div>
        <div class="post-date">
            &nbsp;&nbsp;&nbsp;<code>&nbsp;&nbsp;</code>{
                date.toLocaleDateString(undefined, dateOptions)
            }
        </div>
    </div>
</a>

<style>
    @reference "@styles/base.css";
    .card-container {
        @apply my-2 rounded-xl
        border-2 border-transparent p-2

        text-ctp-rosewater
        transition-all
        duration-200
        hover:border-ctp-sapphire hover:text-ctp-sapphire
        md:w-3xl;
    }

    .post-title {
        @apply text-base;
    }

    .post-date {
        @apply text-sm italic;
    }
</style>
