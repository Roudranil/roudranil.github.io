---
title: "**Bold** *italic* ++underline++ ~~strike~~ `code` title"
subtitle: "Subtitle with **bold**, *italic*, ++underline++, ~~strike~~, and `code`"
description: "Scratch post exercising every layout element, component, and style at once. Delete after review."
date: 2026-08-30T00:00:00Z
draft: true
activeNav: projects
shortTitle: "post-test.md"
seo_keywords:
    - test
    - styling
ai_use:
    - claude
github: "https://github.com/Roudranil/finetuning-llms-for-conversation-in-shakespearean-english"
---

import Block from "@components/Block.astro";

## Heading level two

Body text under h2, with **bold**, *italic*, `inline code`, a [link](https://roudranil.github.io), and ~~GFM strikethrough (won't render — no remark-gfm installed, shown as literal tildes)~~.

### Heading level three

Body text under h3.

#### Heading level four

Body text under h4.

##### Heading level five

Body text under h5.

###### Heading level six

Body text under h6.

## Weird formatting

### Lists

Unordered:

-   First item
-   Second item
    -   Nested item

Ordered:

1.  First step
2.  Second step

### Blockquote

> A blockquote to check quote styling and border color.

### Table

| Column A | Column B |
| -------- | -------- |
| foo      | bar      |
| baz      | qux      |

### Code blocks

Fence with a title (filename tab):

```bash title="example.sh"
echo "hello from a titled code block"
```

Fence without a title (centered language badge only):

```python
def greet(name: str) -> str:
    return f"hello, {name}"
```

### Math ($\KaTeX$)

Inline math: $E = mc^2$.

Block math:

$$
\int_0^\infty e^{-x^2} \, dx = \frac{\sqrt{\pi}}{2}
$$

## Callout blocks

<Block type="info">This is an info callout.</Block>
<Block type="info">This is an `info` callout.</Block>
<Block type="info">This is an info callout. with $\sin(x)$</Block>

<Block type="alert" title="hold on fam we can code">
This is an alert callout.
```python
import numpy as np
```


</Block>

<Block type="tip" title="hey tip here">
This is a tip callout.
</Block>

<Block type="normal" title="can it use math">
This is a normal callout.
$$
\sin^2(x) +\cos^2(x) = 1
$$
dgfdskgjdf
</Block>

## Horizontal rule

---

End of test content.
